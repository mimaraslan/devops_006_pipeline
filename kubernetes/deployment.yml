apiVersion: apps/v1
kind: Deployment
metadata:
  name: devops_006_pipeline
  labels:
    app: devops_006_pipeline
    team: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: devops_006_pipeline
  template:
    metadata:
      labels:
        app: devops_006_pipeline
    spec:
      terminationGracePeriodSeconds: 30
      containers:
      - name: devops_006_pipeline
        image: mimaraslan/devops-006-pipeline:latest
        imagePullPolicy: "Always"
        resources:
          limits:
            memory: "1000Mi"
            cpu: "1000m"
        ports:
        - containerPort: 3000