apiVersion: v1
kind: Service
metadata:
  name: devops_006_pipeline
  labels:
    app: devops_006_pipeline
    team: frontend  
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 3000
  selector:
    app: devops_006_pipeline

